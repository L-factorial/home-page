export default String.raw`# Elastic collision configuration

This animation uses the same elastic-collision algorithm described in the main **Elastic Collision** example.

Only the particle configuration is different: the number, size, mass, starting position, velocity, or visual appearance of the particles has been adjusted to produce this particular demonstration.`;
